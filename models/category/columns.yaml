# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: jetminds.job::lang.models.category.columns.id
        searchable: true

    name:
        label: jetminds.job::lang.models.category.columns.name
        searchable: true

    slug:
        label: jetminds.job::lang.models.category.columns.slug

    created_at:
        label: jetminds.job::lang.models.category.columns.created_at
        type: date

    updated_at:
        label: jetminds.job::lang.models.category.columns.updated_at
        type: timesince
